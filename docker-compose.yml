version: '3.3'
volumes:
  'virtdbnx-asp-home':
    driver: nimble
    driver_opts:
      importVolAsClone: 1-68-virtdbnx-AWSvirtdbnx-asp.docker
      forceImport: "true"
services:
  virtdbnx:
    image: tmeregistry.azurecr.io/virtdbnx-asp
    build:
      context: .
      dockerfile: virtdbnx/Dockerfile_virtdbnx
    depends_on:
      - db
    ports:
      - '50939:80'
      - '2223:2222'
  db:
    image: tmeregistry.azurecr.io/virtdbnx-asp-db
    build:
      context: .
      dockerfile: virtdbnx_db/Dockerfile_virtdbnx_db
    environment:
      SA_PASSWORD: "My_P@ssw0rd!"
      ACCEPT_EULA: "Y"
    ports:
      - '1411:1433'
    volumes:
      - virtdbnx-asp-home:/var/opt/mssql/sqldata/udb
